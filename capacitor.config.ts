import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'za.co.anicontroller.studio',
  appName: 'Anicontroller',
  webDir: 'dist',
  android: {
    backgroundColor: '#090b12',
  },
};

export default config;
