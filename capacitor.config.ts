import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.darren.anicontroller',
  appName: 'Anicontroller',
  webDir: 'dist',
  android: {
    backgroundColor: '#080a12',
  },
};

export default config;
